// const context = ({ req }) => {
//     // You can retrieve user info from the request and provide it to your resolvers if needed
//     const user = getUserFromRequest(req);
  
//     return { user };
//   };
  
//   export default context;
  